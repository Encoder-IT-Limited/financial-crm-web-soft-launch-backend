/** Persistence for admin dashboard. Business rules stay in dashboard.service.ts. */
export const dashboardRepository = {
  // Prisma access is composed from the tenant/public client passed by the service.
};
