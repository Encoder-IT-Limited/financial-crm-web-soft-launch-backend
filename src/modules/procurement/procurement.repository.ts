/** Persistence for purchase orders. Business rules stay in procurement.service.ts. */
export const procurementRepository = {
  // Prisma access is composed from the tenant/public client passed by the service.
};
