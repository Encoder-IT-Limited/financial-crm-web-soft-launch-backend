/** Persistence for invoices. Business rules stay in invoicing.service.ts. */
export const invoicingRepository = {
  // Prisma access is composed from the tenant/public client passed by the service.
};
